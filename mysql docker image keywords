environtment yang terdapat pada image mysql:
1. MYSQL_ROOT_PASSWORD  => untuk melakukan set password yang digunakan oleh mysql
2. MYSQL_ROOT_HOST      => untuk melakukan memberikan pengaturan host apa saja yang boleh konek ke database, ip mana saja yang boleh melakukan akses ke database kita
3. MYSQL_DATABASE
4. MYSQL_USER
5. MYSQL_PASSWORD

docker run --name mysql -e MYSQL_ROOT_PASSWORD=123456 -e MYSQL_ROOT_HOST=% -e MYSQL_DATABASE=coba -v C:/belajar-docker/mysql_data:/var/lib/mysql -d  mysql

note:
-e => environtment
